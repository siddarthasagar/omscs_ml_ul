# Phase 5 — Tasks

**Status:** [ ] NOT STARTED — blocked on Phase 3

- [ ] Implement `WineNN(input_dim)` in `src/supervised/nn_baseline.py`
- [ ] Implement `train_wine_nn(X_train, y_train, X_val, y_val, config)` in `src/supervised/training.py`
- [ ] Implement `plot_f1_comparison(df, out_dir, baseline_df=None)` in `src/utils/plotting.py`
- [ ] Implement `plot_learning_curves(history_df, variant, out_dir)` in `src/utils/plotting.py`
- [ ] Write `scripts/run_phase_5_nn_reduced.py` with logger wired
- [ ] Fill input_dim values in design.md table (from Phase 3 frozen n_components)
- [ ] Run 40 training runs (4 variants × 10 seeds) — produce comparison_table.csv
- [ ] Produce 40 history CSVs
- [ ] Produce phase5_f1_boxplot.png and 4 learning curve PNGs
- [ ] Gate 3 check: confirm identical training config across variants
- [ ] Draft narrative: does DR help or hurt Wine NN? (for report)

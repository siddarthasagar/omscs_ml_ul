# Phase 7 — Tasks (Extra Credit)

**Status:** [ ] OPTIONAL — implement after Phases 3–6 are complete

- [ ] Implement `fit_tsne(X, seed=42)` in `src/unsupervised/reduction.py`
- [ ] Implement `plot_tsne(embedding, labels, title, out_path)` in `src/utils/plotting.py`
- [ ] Write `scripts/run_phase_7_tsne.py` with logger wired
- [ ] Run t-SNE on Wine X_train — produce wine_tsne_labels.png, wine_tsne_clusters.png
- [ ] Run t-SNE on Adult X_train — produce adult_tsne_labels.png, adult_tsne_clusters.png
- [ ] Verify: no t-SNE embedding saved as CSV, no t-SNE used as NN input anywhere
